[pytest]
testpaths = tests
# The suite is CPU-bound DSP on synthesised audio; spreading it over every core
# takes it from ~50 s to ~13 s. Pass `-n 0` to debug a single test serially.
addopts = -q -n auto
filterwarnings =
    ignore::DeprecationWarning:audioread.*
